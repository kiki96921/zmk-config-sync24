# マウス機能の有効化（既存の設定）
CONFIG_ZMK_MOUSE=y

# BluetoothとUSBの基本設定
CONFIG_ZMK_BLE=y
CONFIG_ZMK_USB=y

# 接続の安定性を高める設定
# Bluetoothの送信出力を最大にして、スマホとの接続切れを防ぐ
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# USB接続時にPC側ですぐに認識されるようにするための設定
CONFIG_ZMK_USB_BOOT=y
